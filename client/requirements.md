## Packages
framer-motion | Smooth page transitions and hover effects
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
Font previews rely on the CSS `font-family` matching the `family` field in the database.
Google Fonts are pre-loaded in index.html to support these previews.
